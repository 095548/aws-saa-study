# AWS S3

## S3とは
AWSが提供するオブジェクトストレージサービス
高い耐久性と可用性を兼ね備えているため、データの中長期的な保存に最適なストレージとして使われている。

## S3のデータ保管
・SSE-S3・・・S3の標準暗号方式、S3がマネージドキーを自動で作成・管理し、AES-256を使用。
メリットとしてもっとも簡易で運用負荷が低い。
・SSE-KMS・・・AWS KMSのキー（CMK)を利用、ユーザー側でキーの作成や管理が可能。
・SSE-C・・・クライアントが提供する暗号化キーをリクエストともに送付し、サーバー側で暗号化。
・CSE・・・Amazon S3へ送信する前に、アプリケーション側でデータを暗号化する方式。

クライアントからS3へデータを保管するときに、クライアントからPUTリクエストが送られ、HTTPリクエストヘッダーに暗号方式（SSE-S3、SSE-KMSなど）を指定する情報を付与する。
S3はこのヘッダーを読み取り、指定された方式とキーを使用してデータを暗号化してからディスクに保存する。取得時（GETリクエスト）はクライアントに権限があれば自動的に復号される。

## S3のアクセス管理
S3のアクセス管理にはユーザーベースのIAMポリシーをリソースベースのバケットポリシーとACLを主に利用する。

IAMポリシー・・・
バケットポリシー・・・
ACL・・・
アクセスポイント・・・