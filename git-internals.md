# Gitの内部構造

## Gitとは
巨大なキーバリューストア。キー（ハッシュ）を渡すと、対応する中身（オブジェクト）を返す。

## 3つのオブジェクト
- **ブロブ**：ファイルの中身だけを持つ。ファイル名は持たない。
- **ツリー**：フォルダに対応。中にあるファイル(ブロブ)やサブフォルダ(ツリー)の一覧を、権限・種別・ハッシュ・名前の形で持つ。
- **コミット**：一番上のツリーのハッシュ、親コミットのハッシュ、作者、日時、メッセージを持つ。

## ハッシュ（キー）
- 40文字（SHA-1）。中身から計算されて決まる。
- 中身が同じなら同じハッシュ、1文字でも違えば別のハッシュになる。

## 保存場所
- `.git/objects/` に、中身を圧縮して保存（暗号化ではない）。
- 先頭2文字でフォルダ分け → 1箇所に集中させず分散させるため。
- `git cat-file -p <ハッシュ>` で圧縮を解いた中身が見られる。

## ブランチとHEAD
- ブランチの正体は「最新コミットのハッシュが1行書かれたファイル」（付箋のイメージ）。
- `.git/refs/heads/main` の中身が、コミットのハッシュそのもの。
